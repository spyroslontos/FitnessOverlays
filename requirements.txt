# Core framework
fastapi
uvicorn[standard]
jinja2

# Database
sqlalchemy[asyncio]>=2.0
asyncpg
psycopg2-binary  # Keep for potential sync fallback/migrations

# HTTP Client (async)
httpx

# Session management (Redis-backed)
starlette-session[redis]

# Rate limiting
slowapi

# Environment and config
python-dotenv

# Form handling
python-multipart
