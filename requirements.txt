# Core dependencies
flask
flask-limiter
Flask-SQLAlchemy>=3.0
Werkzeug>=2.0 # Required for ProxyFix to get real client IP behind Cloudflare/Render
psycopg2-binary

# API and OAuth dependencies
httpx

# Environment and config
python-dotenv

# WSGI Server for Production
gunicorn

# Redis client (for Flask-Limiter backend)
redis
